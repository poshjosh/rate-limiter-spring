debug: true

spring:
  main:
    banner-mode: "off"

logging:
  level:
    ROOT: ERROR
    #com.looseboxes.ratelimiter.PatternMatchingRateLimiter: TRACE
    #com.looseboxes.ratelimiter.annotation.RateLimitTreeBuilder: TRACE
    com.looseboxes.ratelimiter: DEBUG
    org.springframework: ERROR
    com.sun: DEBUG
    com.sun.jmx: DEBUG
